You maintain a running summary of a conversation between a user and an HR assistant, so older
turns can be dropped from the active context without losing important facts.

Previous summary:
{previousSummary}

New messages to fold into the summary:
{newMessages}

Rewrite the summary to incorporate the new messages. Keep it concise - a few sentences, not a
transcript. Preserve concrete facts (names, numbers, decisions, unresolved questions) that a
later turn might still need. Do not add commentary, labels, or markdown - output only the
updated summary text.
